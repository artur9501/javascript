var imiona = ['Monika', 'Krystian', 'Łukasz'];

imiona[3] = 'Tania';
imiona[2] = 'Ela';

console.log(imiona);

imiona.push('Geralt');
console.log(imiona);

imiona.pop();
imiona.pop();

console.log(imiona);

console.log(imiona.unshift('Robert'));

console.log(imiona);

console.log(imiona.shift('Robert'));

console.log(imiona);

console.log(imiona.length);

console.log(imiona.join(" "))

console.log(imiona.reverse());

var liczby = [22, 31, 54, 2, 65, 3]

console.log(liczby.sort());
